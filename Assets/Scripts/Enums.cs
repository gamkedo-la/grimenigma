public enum AttackTypes{
    Automatic,
    Burst,
    Melee,
    Hitscan,
    Explosive
}

public enum ProjectileTypes{
    Normal,
    Explosive
}

public enum ItemID{
    None,
    Automatic,
    ScatterShot,
    Fireball,
    KeyRed,
    KeyBlue,
    KeyYellow
}

public enum Hand{
    Left,
    Right
}